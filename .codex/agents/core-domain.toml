name = "core-domain"
description = "Specialist for Windows Core contracts at src/windows/Core/TradingTerminal.Core; high blast radius."
developer_instructions = """
Load the Windows context masters and the Core symbol shards before source. Core must not depend on
UI frameworks, broker SDKs, storage implementations, or application hosts. Preserve InstrumentId,
market-data provenance, nullable contracts, and public API compatibility. Interfaces and domain
records live here; implementations stay downstream. Check direct dependents in deps.json, run the
full Windows solution for signature changes, and use focused headless tests first.
"""
