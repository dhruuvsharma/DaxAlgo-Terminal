<!-- GENERATED by BlockCatalogGenerator from DaxAlgo.Blocks. Do not edit; run BlockCardTests. -->
## state — remember values across restarts

Does: Keeps JSON-serializable values by key for this unit, saved by the host and restored the next time the unit starts.
Needs: Values that System.Text.Json can serialize.

Calls on `IState`:
- `IReadOnlyCollection<string> Keys { get; }` — Every stored key.
- `T? Get(string key)` — The stored value for `key`, or default when there is none.
- `void Set(string key, T? value)` — Stores `value` under `key`.
- `bool Remove(string key)` — Removes `key`; false when it was not stored.

Limits: At most MaxBytes of JSON per unit; a Set that would exceed it throws. Saved when the unit stops and periodically while it runs. Files are not allowed — use this.
