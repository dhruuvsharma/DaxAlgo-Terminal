<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0-windows7.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Exercise the exact WPF-free engine later placed in the bundle. -->
    <ProjectReference Include="..\DaxNewStrategy\Engine\DaxNewStrategy.Engine.csproj" />
    <!-- Tests EXECUTE the kernel, so unlike the plugin project they need the SDK's runtime
         assemblies too (no ExcludeAssets here). -->
    <PackageReference Include="DaxAlgo.Sdk" Version="0.2.0-alpha" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>

</Project>
