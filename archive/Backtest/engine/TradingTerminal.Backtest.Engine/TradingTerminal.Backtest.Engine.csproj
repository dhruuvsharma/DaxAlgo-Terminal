<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Windows deterministic backtest engine. -->
    <TargetFramework>net9.0-windows7.0</TargetFramework>
    <Description>Deterministic event-driven backtest engine (new system): portfolio accounting, simulated order book, pluggable fill/cost models, equity recorder, and report builder. Depends only on Core + MarketData â€” no broker SDKs.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Core\TradingTerminal.Core\TradingTerminal.Core.csproj" />
    <ProjectReference Include="..\..\Pipeline\TradingTerminal.MarketData\TradingTerminal.MarketData.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="TradingTerminal.Tests" />
    <InternalsVisibleTo Include="TradingTerminal.Tests.Headless" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.Reactive" Version="6.0.1" />
  </ItemGroup>

</Project>
