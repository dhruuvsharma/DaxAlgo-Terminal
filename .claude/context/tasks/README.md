# tasks/ — per-change scratchpads

One file per change request: `<YYYY-MM-DD-HHMM>-<slug>.md` (PROTOCOL.md step 6).
Template: Goal · Hypothesis (files, from index/symbols/deps) · Clarifying Qs+answers ·
Plan (ordered) · Blast radius · Build filter · Tests to run · (at end) diff summary.
Cache findings here instead of re-reading files. Safe to prune entries older than a month.
