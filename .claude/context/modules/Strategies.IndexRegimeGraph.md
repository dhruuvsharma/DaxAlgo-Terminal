# Strategies.IndexRegimeGraph — index regime graph

Shape: `modules/Strategies-family.md`. **1,772 LOC / 8 files** (VM 765, window XAML 628).
- Graph/network view of regime relationships across index instruments; bars + L1.
- Surface `symbols/Strategies.IndexRegimeGraph.md` (`IndexRegimeGraphPlugin` @ :14).
- Regime math shared with `Infrastructure/Regime` + `Core` — change the math there, not in the VM.
