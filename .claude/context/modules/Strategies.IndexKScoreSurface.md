# Strategies.IndexKScoreSurface — index K-score surface

Shape: `modules/Strategies-family.md`. **1,721 LOC / 8 files** (VM 861).
- Multi-index K-score surface; math in `Core/IndexKScore/IndexKScoreCalculator.cs` (456 LOC) →
  `symbols/Core-IndexKScore.md`; numeric tests live against Core, not the VM.
- Surface `symbols/Strategies.IndexKScoreSurface.md` (`IndexKScoreSurfacePlugin` @ :8).
- Multi-asset (index family) — check `AssetScope`/broker pills when editing the descriptor.
