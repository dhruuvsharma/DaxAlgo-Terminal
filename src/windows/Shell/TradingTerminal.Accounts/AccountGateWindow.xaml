<mah:MetroWindow x:Class="TradingTerminal.Accounts.AccountGateWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="DaxAlgo Terminal — Account"
                 Width="760"
                 Height="540"
                 ResizeMode="NoResize"
                 ShowMaxRestoreButton="False"
                 ShowMinButton="False"
                 WindowStartupLocation="CenterScreen"
                 Background="{DynamicResource Background.Primary}"
                 Foreground="{DynamicResource Text.Primary}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Padding="34"
                Background="{DynamicResource Background.Elevated}"
                BorderBrush="{DynamicResource Border.Brush}"
                BorderThickness="0,0,1,0">
            <StackPanel VerticalAlignment="Center">
                <Border Width="78"
                        Height="78"
                        HorizontalAlignment="Left"
                        Background="#ECECEC">
                    <Image Width="56"
                           Height="56"
                           Source="pack://application:,,,/TradingTerminal.UI;component/Assets/DaxAlgoLogo.png"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                </Border>
                <TextBlock Margin="0,24,0,0"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Text="DaxAlgo Terminal" />
                <TextBlock Margin="0,8,0,0"
                           Foreground="{DynamicResource Text.Secondary}"
                           TextWrapping="Wrap"
                           Text="One account for the terminal, marketplace purchases, submissions, and reviews." />
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="42,34">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent.Brush}"
                           Text="REQUIRED EDITION" />
                <DockPanel Margin="0,8,0,0">
                    <TextBlock DockPanel.Dock="Right"
                               VerticalAlignment="Bottom"
                               FontSize="16"
                               Foreground="{DynamicResource Text.Secondary}"
                               Text="{Binding PlanPrice}" />
                    <TextBlock FontSize="28"
                               FontWeight="SemiBold"
                               Text="{Binding PlanName}" />
                </DockPanel>
                <TextBlock Margin="0,8,0,0"
                           Foreground="{DynamicResource Text.Secondary}"
                           TextWrapping="Wrap"
                           Text="{Binding PlanSummary}" />
            </StackPanel>

            <Border Grid.Row="1"
                    Margin="0,24,0,0"
                    Padding="16"
                    BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource Background.Surface}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource Border.Brush}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasFailure}" Value="True">
                                <Setter Property="BorderBrush" Value="#90FF8C00" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock FontSize="16"
                               FontWeight="SemiBold"
                               Text="{Binding StatusTitle}" />
                    <TextBlock Margin="0,8,0,0"
                               Foreground="{DynamicResource Text.Secondary}"
                               TextWrapping="Wrap"
                               Text="{Binding StatusMessage}" />
                    <Button Margin="0,12,0,0"
                            Padding="10,5"
                            HorizontalAlignment="Left"
                            Command="{Binding UseAnotherAccountCommand}"
                            Content="Use another account">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanUseAnotherAccount}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <ProgressBar Height="3"
                                 Margin="0,14,0,0"
                                 IsIndeterminate="True">
                        <ProgressBar.Style>
                            <Style TargetType="ProgressBar">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ProgressBar.Style>
                    </ProgressBar>
                </StackPanel>
            </Border>

            <Border Grid.Row="2"
                    Margin="0,16,0,0"
                    Padding="12"
                    VerticalAlignment="Top"
                    Background="#1826A69A"
                    BorderBrush="#5026A69A"
                    BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasEnvironmentNotice}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock FontSize="11"
                               Foreground="{DynamicResource Text.Secondary}"
                               TextWrapping="Wrap"
                               Text="{Binding EnvironmentNotice}" />
                    <Button Margin="0,10,0,0"
                            Padding="10,5"
                            HorizontalAlignment="Left"
                            Command="{Binding ContinueAsLocalDeveloperCommand}"
                            Content="Continue as local developer">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasLocalDeveloperAccess}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>

            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center"
                           FontSize="11"
                           Foreground="{DynamicResource Text.Secondary}"
                           Text="Google OAuth tokens are never stored by this gate." />
                <Button Grid.Column="1"
                        MinWidth="84"
                        Margin="12,0,0,0"
                        Padding="14,7"
                        Command="{Binding CancelCommand}"
                        Content="Cancel" />
                <Button Grid.Column="2"
                        MinWidth="176"
                        Margin="8,0,0,0"
                        Padding="14,7"
                        Command="{Binding ContinueCommand}"
                        Content="{Binding PrimaryActionText}" />
            </Grid>
        </Grid>
    </Grid>
</mah:MetroWindow>
