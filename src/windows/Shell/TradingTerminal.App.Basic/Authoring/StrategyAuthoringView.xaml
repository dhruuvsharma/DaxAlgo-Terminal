<UserControl x:Class="TradingTerminal.App.Authoring.StrategyAuthoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2006"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:uistrat="clr-namespace:TradingTerminal.UI.Strategies;assembly=TradingTerminal.UI"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="B2V" />
    </UserControl.Resources>
    <DockPanel Margin="8">

        <!-- Header: identity + compile -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Id:" VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBox Grid.Column="1" Text="{Binding StrategyId, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" />
            <TextBlock Grid.Column="2" Text="Name:" VerticalAlignment="Center" Margin="12,0,4,0" />
            <TextBox Grid.Column="3" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" />
            <Button Grid.Column="4" Content="Compile &amp; Register" Padding="12,4" Margin="12,0,0,0"
                    Command="{Binding CompileCommand}" />
        </Grid>

        <!-- Status line -->
        <TextBlock DockPanel.Dock="Top" Text="{Binding Status}" Margin="0,0,0,6"
                   TextWrapping="Wrap" FontStyle="Italic" />

        <!-- AI builder: describe a strategy; it writes, compiles, and auto-fixes it. Visible only when a
             codegen provider is wired. The user reviews the generated code, then presses Compile to add
             it (that click is the consent for running model-authored code; it's already scan-gated). -->
        <Border DockPanel.Dock="Top" Margin="0,0,0,8" Padding="8" CornerRadius="3"
                BorderThickness="1" BorderBrush="{DynamicResource Accent.Brush}"
                Background="{DynamicResource Background.Secondary}"
                Visibility="{Binding AiEnabled, Converter={StaticResource B2V}}">
            <StackPanel>
                <TextBlock Text="AI STRATEGY BUILDER" FontWeight="Bold" FontSize="11" Margin="0,0,0,4" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Margin="0,0,6,0" VerticalContentAlignment="Center"
                             Text="{Binding AiPrompt, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Describe the strategy in plain English, e.g. 'a Bollinger-band mean reversion on 20-bar bands'." />
                    <ComboBox Grid.Column="1" Margin="0,0,6,0"
                              ItemsSource="{Binding AiProviders}" SelectedItem="{Binding SelectedAiProvider}"
                              DisplayMemberPath="Label" />
                    <Button Grid.Column="2" Content="Generate" Padding="14,4"
                            Command="{Binding GenerateWithAiCommand}" />
                </Grid>
                <TextBlock Text="{Binding AiStatus}" Margin="0,4,0,0" TextWrapping="Wrap"
                           Foreground="{DynamicResource Text.Secondary}" />
            </StackPanel>
        </Border>

        <!-- Body: editor | (diagnostics + parameters) -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding SourceCode, UpdateSourceTrigger=PropertyChanged}"
                     FontFamily="Consolas" FontSize="12"
                     AcceptsReturn="True" AcceptsTab="True"
                     HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap" />

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

            <DockPanel Grid.Column="2" Margin="6,0,0,0">
                <GroupBox DockPanel.Dock="Top" Header="Diagnostics" MaxHeight="220">
                    <ListView ItemsSource="{Binding Diagnostics}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Sev" Width="60"
                                                DisplayMemberBinding="{Binding Severity}" />
                                <GridViewColumn Header="Ln" Width="40"
                                                DisplayMemberBinding="{Binding Line}" />
                                <GridViewColumn Header="Message" Width="240"
                                                DisplayMemberBinding="{Binding Message}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </GroupBox>

                <GroupBox Header="Parameters">
                    <uistrat:StrategyParameterEditorView DataContext="{Binding Parameters}" />
                </GroupBox>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
