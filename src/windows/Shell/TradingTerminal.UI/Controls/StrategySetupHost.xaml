<UserControl x:Class="TradingTerminal.UI.Controls.StrategySetupHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Root">
    <!-- Full-bleed themed backdrop so the setup screen is never an empty black void; the card is
         centered with a max size and looks intentional when the window is maximized. -->
    <Grid Background="{DynamicResource Background.Primary}">
        <Border MaxWidth="980" MaxHeight="680" Margin="32"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="{DynamicResource Background.Surface}"
                BorderBrush="{DynamicResource Border.Brush}"
                BorderThickness="1" CornerRadius="0"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.5" Color="#000000" />
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left: branding / description / capability pills, with an accent spine. -->
                <Border Grid.Column="0" Background="{DynamicResource Background.Elevated}"
                        CornerRadius="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{DynamicResource Accent.Brush}"
                                CornerRadius="0" />
                        <StackPanel Grid.Column="1" Margin="22,28,20,24" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Subtitle, ElementName=Root}"
                                       Foreground="{DynamicResource Accent.Brush}"
                                       FontSize="11" FontWeight="Bold" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding Title, ElementName=Root}"
                                       Foreground="{DynamicResource Text.Primary}"
                                       FontSize="25" FontWeight="Bold" TextWrapping="Wrap"
                                       Margin="0,0,0,12" />
                            <TextBlock Text="{Binding Description, ElementName=Root}"
                                       Foreground="{DynamicResource Text.Secondary}"
                                       FontSize="13" TextWrapping="Wrap" />
                            <ItemsControl ItemsSource="{Binding TagItems, ElementName=Root}" Margin="0,16,0,0">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource Background.Surface}"
                                                BorderBrush="{DynamicResource Accent.Brush}"
                                                BorderThickness="1" CornerRadius="0"
                                                Padding="9,2" Margin="0,6,6,0">
                                            <TextBlock Text="{Binding}" FontSize="10.5" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource Accent.Brush}" />
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Right: the per-strategy form, supplied as the control's content. -->
                <Grid Grid.Column="1" Margin="26,26,26,22">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Heading, ElementName=Root}"
                               Foreground="{DynamicResource Text.Secondary}"
                               FontSize="12" FontWeight="SemiBold" Margin="0,0,0,14" />
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ContentPresenter Content="{Binding Form, ElementName=Root}" />
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
