<UserControl x:Class="TradingTerminal.App.Plugins.PluginManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignWidth="780" d:DesignHeight="520">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Strategy plugins" FontSize="18" FontWeight="Bold" />
            <TextBlock Text="{Binding TrustPolicySummary}" Opacity="0.8" Margin="0,4,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,14,0,0">
            <Button Content="Install plugin…" Command="{Binding InstallPluginCommand}" Padding="14,6" Margin="0,0,8,0" />
            <Button Content="Open plugins folder" Command="{Binding OpenPluginsFolderCommand}" Padding="14,6" />
        </StackPanel>

        <TextBlock Grid.Row="2" Margin="0,10,0,0" Opacity="0.65" TextWrapping="Wrap" FontSize="11">
            <Run Text="Plugins folder:  " /><Run Text="{Binding PluginsRoot, Mode=OneWay}" />
        </TextBlock>

        <!-- Loaded plugins -->
        <GroupBox Grid.Row="3" Header="Loaded plugins" Margin="0,14,0,0">
            <ListView ItemsSource="{Binding LoadedPlugins}" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="260" />
                        <GridViewColumn Header="SDK" DisplayMemberBinding="{Binding TargetSdkVersion}" Width="110" />
                        <GridViewColumn Header="Assembly" DisplayMemberBinding="{Binding AssemblyPath}" Width="360" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Status -->
        <TextBlock Grid.Row="4" Text="{Binding Status}" Margin="0,12,0,0" TextWrapping="Wrap" FontWeight="SemiBold" />

        <!-- Developer hint -->
        <TextBlock Grid.Row="5" Margin="0,10,0,0" Opacity="0.6" TextWrapping="Wrap" FontSize="11"
                   Text="Developing a strategy? Reference the DaxAlgo.Sdk / DaxAlgo.Sdk.Wpf packages, implement IStrategyPlugin (see samples/DaxAlgo.SamplePlugin), build, then Install it here or drop the folder into the plugins directory and restart. See docs/plugins.md." />
    </Grid>
</UserControl>
