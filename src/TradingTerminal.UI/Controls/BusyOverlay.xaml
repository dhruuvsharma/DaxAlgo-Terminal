<UserControl x:Class="TradingTerminal.UI.Controls.BusyOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <!-- Dim backdrop that also swallows clicks while busy. Visibility follows IsActive. -->
    <Grid Background="#C8050608"
          Visibility="{Binding IsActive, ElementName=Root, Converter={StaticResource BoolToVis}}">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                CornerRadius="10" Padding="30,24" MinWidth="320"
                Background="{DynamicResource Background.Surface}"
                BorderBrush="{DynamicResource Border.Brush}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="0" Opacity="0.7" />
            </Border.Effect>
            <StackPanel HorizontalAlignment="Center">
                <!-- Self-contained sweeping-arc spinner (a faint ring + a ~70% arc rotated forever). The
                     arc length is tuned for this fixed 46px diameter; StrokeDashArray is in stroke-units. -->
                <Grid Width="46" Height="46" HorizontalAlignment="Center">
                    <Ellipse Stroke="#1FFFFFFF" StrokeThickness="4" />
                    <Ellipse Stroke="{DynamicResource Accent.Brush}" StrokeThickness="4"
                             StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                             StrokeDashArray="25.3 10.8"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform Angle="0" />
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                            From="0" To="360" Duration="0:0:0.9" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>
                <TextBlock Text="{Binding Title, ElementName=Root}"
                           Foreground="{DynamicResource Text.Header}"
                           FontSize="15" FontWeight="SemiBold"
                           HorizontalAlignment="Center" TextAlignment="Center"
                           Margin="0,18,0,0" />
                <TextBlock Text="{Binding Message, ElementName=Root}"
                           Foreground="{DynamicResource Text.Secondary}"
                           FontSize="11.5" TextWrapping="Wrap" MaxWidth="360"
                           HorizontalAlignment="Center" TextAlignment="Center"
                           Margin="0,6,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
