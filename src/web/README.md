# src/web — future web UI head (placeholder)

Reserved for a future web front-end (e.g. Blazor / ASP.NET) built on the same portable core in
`src/shared/`. Nothing here yet — see `linux/README.md` for the platform split and roadmap.
